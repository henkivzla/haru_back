PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_clave_mysql
DB_NAME=lilit_db
JWT_SECRET=tu_clave_secreta_jwt
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# Correo (opcional en local — sin SMTP el enlace se imprime en consola del backend)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@lilit.ve

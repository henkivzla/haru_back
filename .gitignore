# Dependencies
node_modules/

# Environment (never commit secrets)
.env
.env.*
!.env.example
!.env.production.example

# Internal docs (local only — see docs/README.md)
docs/

# Logs
*.log
npm-debug.log*

# Build / coverage
dist/
coverage/

# Uploaded product images (keep folder marker)
uploads/*
!uploads/.gitkeep

# OS / editor
.DS_Store
.idea/
.vscode/
*.sw?

# SQL — aplicar manualmente en MySQL/phpMyAdmin (no versionar)
*.sql
**/*.sql